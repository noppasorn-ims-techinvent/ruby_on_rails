<div id="<%= dom_id cat %>" class="card mb-3">
  <div class="card-body">
    <p class="card-text">
      <strong>Name:</strong>
      <%= cat.name %>
    </p>

    <p class="card-text">
      <strong>Color:</strong>
      <%= cat.color %>
    </p>
  </div>
</div>
