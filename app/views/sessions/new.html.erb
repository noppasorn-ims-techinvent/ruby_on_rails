<div class="container my-4">
<div class="card">
  <div class="card-header">
    <h2><%= content_for?(:title) ? yield(:title) : "Login" %></h2>
  </div>
  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>
</div>